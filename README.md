## 🚀Agent Scaffolding

- **Logging System**: Console output and persistent file storage.

- **ORM Framework Encapsulation**: Based on SQLAlchemy, supporting SQLite and MySQL database operations for the time being.

- **FAISS-based Similarity Retrieval**: Enhancement Example for similarity retrieval.

- **LangGraph-based Workflow Orchestration**: Along with tool invocation and tool registry cases.

- **gRPC-based** High-performance Service Encapsulation.

- **LLM API Secondary Encapsulation**: JSON parsing, timeout retry, and error handling.

